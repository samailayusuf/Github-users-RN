import React from 'react'
import { View, TextInput } from 'react-native'

const Form = ({ navigation }) => {
    return (
        <View>
            <Text> Form</Text>
        </View>
    )
}

export default Form
